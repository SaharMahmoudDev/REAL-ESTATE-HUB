// import React from "react";
    
// // EXTARNAL COMPONENTS
// import { MenuItem } from "@mui/material";

// const NumericOptions = ({ to, from }) => {
//   return (
//     <>
//       {Array.from({ length: to - from + 1 }, (_, i) => (
//         <MenuItem key={i + from} value={`${+(i + from)}`}>
//           {i + from}
//         </MenuItem>
//       ))}
//     </>
//   );
// };

// export default NumericOptions;
